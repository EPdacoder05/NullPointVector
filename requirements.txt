# Core dependencies for IDPS functionality
python-dotenv>=1.0.0
PyYAML>=6.0
# 43.0.1 had HIGH CVEs (GHSA-537c-gmf6-5ccf, CVE-2026-26007, …).
# 48.0.1 patches those. Re-verify `from cryptography.fernet import Fernet`
# on linux/aarch64 if you bump further (old 44.x wheels SIGILL'd on some ARM).
cryptography==48.0.1
watchdog>=3.0

# Email processing
imaplib2>=3.6
email-validator>=2.0

# ML and data processing
pandas>=2.0
# Pinned to the stack the champion pickles were trained under
# (`models/REPRO_MANIFEST.json`). A floating range lets a Render rebuild resolve
# a newer sklearn/numpy than the artifacts, which unpickles with
# InconsistentVersionWarning ("might lead to ... invalid results") and silently
# changes verdicts. Bump these and the manifest together, via a gated retrain.
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
torch>=2.0
transformers>=4.30
sentence-transformers>=2.2

# Database
psycopg2-binary>=2.9
sqlalchemy>=2.0

# Security and validation
bleach>=6.0
passlib>=1.7
argon2-cffi>=23.1
bcrypt>=4.0

# Web / HTTP clients (Signal Deck is Jinja on FastAPI — no Dash)
requests>=2.31.0

# API framework
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2.0
PyJWT[crypto]>=2.8                  # JWT auth (no ecdsa / CVE-2024-23342)
python-multipart>=0.0.6             # OAuth2 password form parsing
prometheus-client>=0.19            # /metrics + latency histograms
redis>=5.0                          # shared rate-limit + idempotency at scale
gunicorn>=21.0                      # multi-worker API (parallel CPU)

# CLI interface
rich>=13.0

# Offensive intelligence
dnspython>=2.0
python-whois>=0.8
