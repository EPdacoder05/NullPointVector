# Core dependencies for IDPS functionality
python-dotenv>=1.0.0
PyYAML>=6.0
# Pinned: the latest cryptography wheel SIGILLs (Illegal instruction) on some
# ARM hosts (Colima/Apple-Silicon VM, Graviton) due to advanced SIMD codegen.
# 43.0.1 is verified-good across x86_64 and aarch64. Do not loosen without
# re-verifying `from cryptography.fernet import Fernet` on ARM.
cryptography==43.0.1
watchdog>=3.0

# Email processing
imaplib2>=3.6
email-validator>=2.0

# ML and data processing
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
torch>=2.0
transformers>=4.30
sentence-transformers>=2.2

# Database
psycopg2-binary>=2.9
sqlalchemy>=2.0

# Security and validation
bleach>=6.0
passlib>=1.7
bcrypt>=4.0

# Web interface (Dash/Plotly)
dash>=2.14
plotly>=5.0
dash-bootstrap-components>=1.5
requests>=2.31.0

# API framework
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2.0
python-jose[cryptography]>=3.3      # JWT auth
python-multipart>=0.0.6             # OAuth2 password form parsing
prometheus-client>=0.19            # /metrics + latency histograms
redis>=5.0                          # shared rate-limit + idempotency at scale
gunicorn>=21.0                      # multi-worker API (parallel CPU)

# CLI interface
rich>=13.0

# Testing
pytest>=7.4
pytest-cov>=4.1

# Code quality
black>=23.0
flake8>=6.0

# Offensive intelligence
dnspython>=2.0
python-whois>=0.8